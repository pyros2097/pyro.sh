{
  "name": "timer.pyro.sh",
  "scripts": {
    "css": "echo '<style>' > src/components/styles.html && tailwindcss-cli build ./config.css >> ./src/components/styles.html && echo '</style>' >> src/components/styles.html",
    "compile": "sergey --root=./src/ --imports=./components --output=../public --port=1237",
    "start": "npm run compile -- --watch",
    "build": "NODE_ENV=production npm run css && npm run compile",
    "build-app": "tauri build"
  },
  "devDependencies": {
    "sergey": "0.0.13",
    "tailwindcss-cli": "^0.1.2"
  },
  "dependencies": {
    "tauri": "^0.14.1"
  }
}
