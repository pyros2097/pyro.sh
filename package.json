{
  "name": "pyros.sh",
  "scripts": {
    "start": "serve .",
    "build": "tailwindcss-cli build ./assets/config.css -o ./assets/styles.css",
    "build-prod": "npm run build --production"
  },
  "devDependencies": {
    "serve": "^11.3.2",
    "tailwindcss-cli": "^0.1.2"
  }
}